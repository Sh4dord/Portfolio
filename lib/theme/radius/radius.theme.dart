import 'package:flutter/material.dart';

class RadiusTheme {
  final Radius xl = const Radius.circular(32);
  final Radius l = const Radius.circular(24);
  final Radius m = const Radius.circular(16);
  final Radius s = const Radius.circular(8);

  final BorderRadius borderRadiusXL = BorderRadius.circular(32);
  final BorderRadius borderRadiusL = BorderRadius.circular(24);
  final BorderRadius borderRadiusM = BorderRadius.circular(16);
  final BorderRadius borderRadiusS = BorderRadius.circular(8);
}
